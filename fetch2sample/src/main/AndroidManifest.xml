<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tonyodev.fetch2sample">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SingleDownloadActivity" />

        <activity android:name=".DownloadListActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".GameFilesActivity"
            android:screenOrientation="landscape"/>

        <activity android:name=".MultiEnqueueActivity" />

        <activity android:name=".FragmentActivity" />

    </application>
</manifest>
